---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: team6-eureka-client-gateway-server
  namespace: group1-team6
spec:
  replicas: 1
  selector:
    matchLabels:
      app: team6-eureka-client-gateway-server
  template:
    metadata:
      labels:
        app: team6-eureka-client-gateway-server
    spec:
      containers:
      - name: team6-eureka-client-agateway-server
        image: k8s-vga-worker1:5000/group1-team6-eureka-client-gateway-server
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 9202
---
apiVersion: v1
kind: Service
metadata:
  name: team6-eureka-client-gateway-server
  namespace: group1-team6
spec:
  selector:
    app: team6-eureka-client-gateway-server
  ports:
    - protocol: TCP
      port: 9010
      targetPort: 9010
      nodePort: 30615
  type: NodePort
